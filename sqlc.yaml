version: "2"
sql:
  - engine: sqlite
    queries: sql/query.sql
    schema: sql/schema.sql
    gen:
      go:
        package: store
        out: store
        emit_json_tags: true
        emit_pointers_for_null_types: true
